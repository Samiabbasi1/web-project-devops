apiVersion: v1
kind: Service
metadata:
  name: my-web-app
  labels:
    app: my-web-app
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: my-web-app
  type: ClusterIP